


# Smart Attendance System — Requirements
# Install: pip install -r requirements.txt

# ── Core Flask ──
flask==3.0.0
flask-sqlalchemy==3.1.1
sqlalchemy==2.0.23

# ── Primary Face Recognition Stack ──
insightface==0.7.3          # ArcFace via buffalo_l — best for glasses/low-light/distance
onnxruntime        # ONNX runtime for InsightFace inference

# ── Fallback Face Recognition ──
deepface==0.0.79            # ArcFace fallback
tf-keras                    # Required by deepface

# ── Computer Vision ──
opencv-python==4.8.1.78
numpy==1.24.4
Pillow==10.1.0

# ── Augmentation Pipeline (critical for model robustness) ──
albumentations==1.3.1       # GPU-free image augmentation
# If albumentations install fails, the system uses manual fallback augmentation

# ── ML Utilities ──
scikit-learn==1.3.2         # Cosine similarity metrics

# ── Anti-Spoofing (Optional — install Silent-Face separately) ──
# git clone https://github.com/minivision-ai/Silent-Face-Anti-Spoofing
# Copy: src/ → models/silent_face/src/
# Copy: resources/anti_spoof_models/ → models/anti_spoof_models/
# torch>=1.8.0              # Required by Silent-Face

# ── Utilities ──
requests==2.31.0
python-dotenv==1.0.0

setuptools>=65.0.0
wheel

gunicorn
